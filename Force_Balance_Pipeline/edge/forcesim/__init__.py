"""forcesim: the probe's reading generator (doc 04). Standard library only, so it runs on the Pi.

The live probe simulator and the 90-day backfill generator import the same code from here (doc 07,
Phase 2): the calibrated walk (walk.py), the signature definitions shared with the control topic
(signatures.py), and the doc 02 envelope with its seedable ULID (envelope.py).
"""
