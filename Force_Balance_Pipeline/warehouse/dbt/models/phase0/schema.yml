version: 2

seeds:
  - name: phase0_seed
    columns:
      - name: id
        data_tests:
          - not_null
          - unique

models:
  - name: phase0_model_a
    columns:
      - name: id
        data_tests:
          - not_null
          - unique
  - name: phase0_model_b
    columns:
      - name: id
        data_tests:
          - not_null
          - unique
