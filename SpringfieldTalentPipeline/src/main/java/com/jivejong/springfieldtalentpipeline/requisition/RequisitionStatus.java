package com.jivejong.springfieldtalentpipeline.requisition;

/** Lifecycle of a job opening - unrelated to a candidate's pipeline stage. */
public enum RequisitionStatus {
    OPEN,
    FILLED,
    CANCELLED
}
